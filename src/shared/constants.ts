type DefaultLocale = 'ru';
type Locales = DefaultLocale | 'en';

const locales: Locales[] = ['ru', 'en'];
const defaultLocale: DefaultLocale = 'ru';

export { locales, defaultLocale, type Locales, type DefaultLocale };
